# Minimal requirements for Windows demo
# Install these FIRST before anything else

# Core Framework - EXACT versions for compatibility
fastapi==0.109.0
pydantic==2.5.3
pydantic-core==2.14.6
pydantic-settings==2.1.0
uvicorn[standard]==0.27.0

# Essential dependencies
redis==5.0.1
httpx==0.25.2
aiohttp==3.9.1
python-dotenv==1.0.0
pyyaml==6.0.1

# Optional (comment out if causing issues)
# paramiko==3.4.0  # For Unix SSH - not needed for demo
# cx_Oracle==8.3.0  # For Oracle - not needed for demo
